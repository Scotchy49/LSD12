program x;
  {this is a comment}
  function main(): void;
    var
      a int;
      b bool;

      function fct1() : int;
        var
          a int;
        begin
          a := 1;
          return fct1();
        end;
    begin
      b || false;
      a := 0;
      b := false; 
      b := b || true || a = a || a <= 2;
      if( b  ) then
        write 1;
      fi;

      while( b ) do
        if( b ) then
            a := a + 1;
            if( b ) then
                a := a + 1;
                if( b ) then
                    a := a + 1;
                fi;
            else
                a := a - 1;
            fi;
        fi;
        write a;
      od;
    end;
end;
