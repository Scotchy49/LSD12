program x;
  {this is a comment}
  function main(): void;
    var
      a int;
      b bool;
      function fct0() : int;
        var
          a bool;
          b bool;
        begin
          if( a = b ) then
            fct0();
          fi;
          return 1;
        end;
      begin
        a := fct0();
        b := a <= 0;
        write a;
      end;
end;
